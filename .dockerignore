# Put only the CI-prebuilt binary and the example config into the build
# context, and exclude everything else (target/, .git, etc., possibly several
# GB) to avoid slowing down docker build.
*
!dnsbuffer
!config.example.toml
